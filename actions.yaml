# Copyright 2022 Penny Gale
# See LICENSE file for licensing details.

restart:
  description: Restarts the example service
  params:
    delay:
      description: "Introduce an artificial delay (for testing)."
      type: integer
      default: 0

custom-restart:
  description: Restarts the example service
  params:
    delay:
      description: "Introduce an artificial delay (for testing)."
      type: integer
      default: 0

get-lock-status:
  description: |
    Must be executed in the leader. Returns the overall lock status of the cluster. 

share-useless-data:
  description: |
    Triggers a relation-changed by sending some useless data to the peers.